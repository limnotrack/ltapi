% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lt_fetch.R
\name{lt_fetch}
\alias{lt_fetch}
\title{Query LimnoTrack database with flexible parameters}
\usage{
lt_fetch(
  table,
  select = NULL,
  filter = NULL,
  order = NULL,
  limit = 1000L,
  offset = 0L,
  base_url = "https://api.limnotrack.com/postgrest"
)
}
\arguments{
\item{table}{character; name of the table to query (e.g., \code{"lake_contours"}).}

\item{select}{character; comma-separated column names to select
(e.g., \code{"col1,col2"}). If \code{NULL}, all columns are returned.}

\item{filter}{named list of PostgREST filter expressions, typically built
with \code{\link[=lt_filter]{lt_filter()}} (e.g., \code{lt_filter(lernzmp_id == "LID40188")}).}

\item{order}{character; column ordering (e.g., \code{"col1.asc,col2.desc"}).}

\item{limit}{integer; maximum number of records to return. Default \code{1000}.}

\item{offset}{integer; number of records to skip for pagination. Default \code{0}.}

\item{base_url}{character; base URL of the LimnoTrack PostgREST API.}
}
\value{
A \link[tibble:tibble]{tibble::tibble}, or an \link[sf:sf]{sf::sf} object if the table contains
geometry data.
}
\description{
Query any table in the LimnoTrack database with optional parameters for
selecting columns, filtering rows, ordering results, and pagination.
Automatically detects geometry columns and returns an \link[sf:sf]{sf::sf} object if
present, or a \link[tibble:tibble]{tibble::tibble} otherwise.
}
\section{Errors}{

\describe{
\item{\code{ltapi_api_unavailable}}{The API could not be reached, or returned a 502/503/504.}
\item{\code{ltapi_api_error}}{The API returned an unexpected HTTP error.}
}
}

\examples{
\dontrun{
  lt_fetch(
    table  = "lake_contours",
    filter = lt_filter(lernzmp_id == "LID40188")
  )
}

}
